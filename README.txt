Download repo with:
    $git clone https://github.com/NotSoup/Assgn1.git

First install the required packages by running:
    $pip install -r requirements.txt

Run all learning by running:
    $python run_experiments.py